# vim: shiftwidth=2:
box: wercker/python
build:
  steps:
    - script:
        name: install apt dependencies
        code: sudo apt-get install libreadline-dev
    - pip-install
    - script:
        name: flask tests
        code: python app_test.py
